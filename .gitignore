# ESP-IDF build output
build/
# release.sh output; rebuildable from any tag
dist/
sdkconfig
sdkconfig.old
managed_components/
dependencies.lock

# Editor / OS
.vscode/
.DS_Store
*.swp

# Python
__pycache__/
*.pyc
*.jsonl
# ...but keep the archived runs the docs draw their charts and numbers from,
# otherwise docs/regen.sh cannot be run from a fresh clone and every measured
# claim in DEVELOPMENT_PLAN.md S5.9 is unverifiable. ~310 KB total.
!discharge-run*.jsonl
discharge.err
dashboard.err
*.html
calibration.json
