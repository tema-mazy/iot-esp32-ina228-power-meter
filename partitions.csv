# Name,     Type, SubType, Offset,   Size,     Flags
# Dual OTA app slots for ATFW updates over the serial link (plan S2.5).
# No factory partition: with two OTA slots it would only waste flash.
# nvs is 24K rather than the usual 16K -- the fuel gauge writes every ~30 s,
# so extra pages spread the erase cycles (plan S4, NVS write policy).
nvs,        data, nvs,      0x9000,   0x6000,
otadata,    data, ota,      0xf000,   0x2000,
phy_init,   data, phy,      0x11000,  0x1000,
ota_0,      app,  ota_0,    0x20000,  0x1B0000,
ota_1,      app,  ota_1,    0x1D0000, 0x1B0000,
